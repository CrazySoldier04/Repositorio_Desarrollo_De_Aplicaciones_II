using System;
using System.Collections.Generic;
using System.Text;

namespace Personas_Ejercicio
{
    class Checador
    {
        public DateTime fecha;
        public DateTime hora;
        public enum Edificio {A, B, C, D, E, F, G, H}

        public bool Checar(string numEmpleado)
        {
            bool res = false;
            return res;
        }

        public bool Registrar(string numEmpleado)
        {
            bool res = false;
            return res;
        }

        public bool BorrarHuella(string numEmpleado)
        {
            bool res = false;
            return res;
        }
    }
}
